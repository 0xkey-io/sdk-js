{
  "name": "@0xkey-io/example-deployer",
  "version": "0.1.7",
  "private": true,
  "scripts": {
    "start": "pnpm -w run build-all && tsx src/index.ts",
    "compile": "tsx src/compile.ts",
    "clean": "rimraf ./dist ./.cache",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@0xkey-io/sdk-server": "workspace:*",
    "@0xkey-io/ethers": "workspace:*",
    "dotenv": "^16.0.3",
    "ethers": "^6.10.0",
    "solc": "0.8.13"
  }
}
